describe('Register', function () {

  describe('On submitting form', function () {

    beforeEach(function () {
      // //jasmine.Ajax.install();
      // //jasmine.getFixtures().fixturesPath = 'app/views'
      // //loadFixtures('index.html');
      // jasmine.Ajax.install();
      // jasmine.Ajax.stubRequest('/users/new');
      // // var $button = $('<form method="post" class="register" id="register"><input type="text" name="username"><button id="register_button">Register</button></form>');
      // //$button.prependTo('body');
      // expect($('#register_button').text()).toEqual('Register');
    });

    it('sends an ajax request', function () {
      // spyOn($, 'ajax')
      // $('#register_button').click();
      // expect($.ajax).toHaveBeenCalled();
      // //expect($.ajax.calls.mostRecent().args[0]['url']);
      // //expect($.ajax.mostRecentCall.args).toEqual()
      // jasmine.Ajax.uninstall();
    });

  });

});